#ifndef __SHADOWS
#define __SHADOWS

static const float BlendThreshold = 0.3f;
static const float Bias = 0.005f; // Bias to prevent shadow acne
static const uint NumCascades = 4;

// TODO: copy from shadows.hlsl from the old project and check how to deal with the array and constants/parameters
#endif
